[package]
name = "garu-wasm"
version = "0.5.3"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
garu-core = { path = "../garu-core" }
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
serde-wasm-bindgen = "0.6"
